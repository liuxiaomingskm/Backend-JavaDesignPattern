package com.example.demo.Concept;

public class Concept {
}

/**
 * Visitor:
 * A pattern where a component(visitor) is allowed to traverse the entire inheritance hierarchy. Implemented by propagating
 * a single visit() method throughout the entire hierarchy.
 */
